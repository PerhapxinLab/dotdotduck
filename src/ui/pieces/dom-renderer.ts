/**
 * Back-compat shim — implementation lives in `./dom/index.ts`.
 * External imports of `pieces/dom-renderer` still resolve here.
 */
export { PieceDomRenderer } from './dom';
export type { PieceDomRendererOptions } from './dom';
