/**
 * Toolbox-internal shared types (Doc / Adapter / Combiner / sync payloads).
 * No language or scoring logic — those live in `dddk/utils/text` so the
 * agent layer can use them without depending on toolbox.
 */
export * from './types.js';
